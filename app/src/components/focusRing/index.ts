/**
 * FocusRing Component
 *
 * Basic implementation of a reusable focus ring component for SVG elements.
 * This version always renders rectangular focus rings and requires explicit
 * element dimensions and positioning.
 */

export { FocusRing } from './focusRing';
export type { FocusRingProps, FocusRingRendererProps } from './focusRing.types';
